import * as React from 'react';
import './index.css';

const Partners: React.StatelessComponent = () => {
    return (
        <div className="ui container">
            Partners
        </div>
    );
};

export default Partners;
